# SC'24 (Wu et al.): the GE tools live in app/GE (they are the GE application); this folder holds the parallel
# evaluation drivers with ADIOS2 I/O, built when MPI, ADIOS2 and SZ3 are present.
add_subdirectory(parallel)
