# Paper artifacts: evaluation drivers kept to reproduce published experiments. Built only with
# -DPRODM_BUILD_ARTIFACTS=ON (build_script.sh turns it on); each folder checks its own prerequisites.
add_subdirectory(SC-24)
add_subdirectory(HPDC-26)
add_subdirectory(SC-26)
